# Dépendances WatermarkCleaner
# NOTE : torch / torchvision / CUDA / ffmpeg sont DÉJÀ fournis par Colab -> ne pas les réinstaller.
# Les dépendances spécifiques à ProPainter sont installées séparément via ProPainter/requirements.txt
# (voir la cellule d'installation du notebook).

# gradio 4.44.1 importe HfFolder depuis huggingface_hub -> on fige une version de
# huggingface_hub qui le contient encore (les versions >=1.0 l'ont supprimé).
gradio==4.44.1
gradio_client==1.3.0
huggingface_hub==0.25.2
# Gradio 4.44 est incompatible avec Starlette 1.0 (bug TemplateResponse
# "unhashable type: dict") -> on fige la pile web sur une version qui marche.
fastapi==0.112.2
starlette==0.37.2
opencv-python-headless>=4.8
Pillow>=9.5
numpy>=1.24
tqdm>=4.65
# Détection automatique du texte (filigranes) sur GPU
easyocr>=1.7
# Amélioration de netteté finale (Real-ESRGAN). Cohabite avec Gradio TANT QUE
# Starlette reste figé ci-dessus (sinon son install fait remonter Starlette en 1.0).
realesrgan>=0.3.0
